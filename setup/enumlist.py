from enum import Enum
#
class ConnectionParams(Enum):
    POSTGRES_DB = mt-pg
    POSTGRES_USER = user
    POSTGRES_PASSWORD = password
    MYSQL_ROOT_PASSWORD = password
    MYSQL_HOSTNAME = root
    MYSQL_DATABASE=mt-mysql
